import RegisterForm from "../form/RegisterForm";

const RegisterPage = () => {
  return (
    <div className="flex flex-col h-screen items-center w-screen justify-center">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
